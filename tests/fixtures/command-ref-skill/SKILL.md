---
name: command-ref-skill
description: This skill shows a command line in inline code, naming a bundled script followed by a flag. The flag is not part of the file name, so both validators must reference only the script itself, which is present in this skill.
version: 1.0.0
---

## Instructions

Run `scripts/build.sh --watch` to rebuild on every change.
